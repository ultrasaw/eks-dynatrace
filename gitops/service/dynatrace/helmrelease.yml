apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: dynatrace-operator
spec:
  interval: 5m
  chart:
    spec:
      chart: dynatrace-operator
      version: "1.4.1"
      sourceRef:
        kind: HelmRepository
        name: dynatrace-operator
